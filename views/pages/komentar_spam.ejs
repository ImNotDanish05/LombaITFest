<!DOCTYPE html>
<html>
<head>
  <title>Komentar Spam</title>
  <%- include('../partials/headerlogin', { user }) %>
  <style>
    body {
      background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460, #2e5e8e);
      background-size: 400% 400%;
      animation: animated-gradient 15s ease infinite;
      font-family: 'Poppins', sans-serif;
      color: white;
      margin: 0;
      padding: 0;
    }

    @keyframes animated-gradient {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .container-flex {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      box-sizing: border-box;
    }

    .glass-card {
      width: 100%;
      max-width: 700px;
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(12px);
      border-radius: 25px;
      border: 1px solid rgba(255, 255, 255, 0.15);
      box-shadow: 0 8px 32px rgba(0, 201, 167, 0.4), 0 0 20px rgba(58, 134, 255, 0.3);
      color: white;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      padding: 2rem 2.5rem;
      box-sizing: border-box;
    }

    .glass-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 16px 40px rgba(58, 134, 255, 0.5), 0 0 30px rgba(0, 201, 167, 0.4);
    }


    .warning {
      color: #ffcc00;
      font-weight: bold;
      margin-bottom: 10px;
      text-align: center;
    }

    h1 {
      font-size: 1.8rem;
      text-align: center;
      margin-bottom: 20px;
    }

    .btn-custom-action {
      background: linear-gradient(
      120deg,
      #FF1E1E,
      #9F0000,
      rgba(0, 0, 0, 0.2),
      #9F0000,
      #FF1E1E
    );
    background-size: 300% 100%;
    animation: animate-bg 5s ease infinite;
    border: none;
    border-radius: 12px;
    color: white;
    font-weight: 500;
    padding: 12px 25px;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
}

@keyframes animate-bg {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}


  .btn-custom-action:hover {
    transform: scale(1.05);
    box-shadow: 0 6px 20px rgba(255, 0, 0, 0.5);
  }

    ul {
      list-style: none;
      padding-left: 0;
    }

    li {
      margin-bottom: 8px;
    }

    #deleteBtn:disabled {
      background: #ccc;
      color: #888;
      cursor: not-allowed;
    }

    .container-flex {
      min-height: 80vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
  </style>
</head>
<body>
  <div class="container-flex">
    <div class="glass-card">
      <h1>🚨 Ditemukan <%= comments.length %> komentar spam</h1>

      <% if (!user.isOwner) { %>
        <div class="warning">
          ⚠️ Kamu bukan pemilik video ini. Kamu hanya bisa melaporkan komentar sebagai spam, bukan menghapusnya permanent.
        </div>
      <% } %>

      <form method="POST" action="/delete-comments" id="deleteForm">
        <input type="hidden" name="isOwner" value="<%= user.isOwner ? '1' : '0' %>">

        <label>
          <input type="checkbox" id="selectAll"> Pilih Semua
        </label>

        <ul class="mt-3">
          <% comments.forEach(function(comment) { %>
            <li>
              <label>
                <input type="checkbox" class="comment-checkbox" name="ids" value="<%= comment.commentId %>">
                <b><%= comment.author %>:</b> <%= comment.text %>
              </label>
            </li>
          <% }); %>
        </ul>

        <div class="text-center mt-4">
          <% if (user.isOwner) { %>
            <button type="submit" id="deleteBtn" class="btn-custom-action" disabled>Hapus Komentar Terpilih</button>
          <% } else { %>
            <button type="submit" id="deleteBtn" class="btn-custom-action" disabled>Report sebagai Spam</button>
          <% } %>
        </div>
      </form>
    </div>
  </div>

  <!-- SCRIPT checkbox logic -->
  <script>
    const selectAll = document.getElementById('selectAll');
    const checkboxes = document.querySelectorAll('.comment-checkbox');
    const deleteBtn = document.getElementById('deleteBtn');

    document.getElementById('deleteForm').addEventListener('submit', function(e) {
      if (document.querySelectorAll('.comment-checkbox:checked').length === 0) {
        e.preventDefault();
        alert('Pilih minimal 1 komentar dulu!');
      }
    });

    function updateDeleteBtn() {
      deleteBtn.disabled = document.querySelectorAll('.comment-checkbox:checked').length === 0;
    }

    selectAll.addEventListener('change', function() {
      checkboxes.forEach(cb => cb.checked = selectAll.checked);
      updateDeleteBtn();
    });

    checkboxes.forEach(cb => {
      cb.addEventListener('change', function() {
        updateDeleteBtn();
        if (!this.checked) selectAll.checked = false;
        else if (document.querySelectorAll('.comment-checkbox:checked').length === checkboxes.length)
          selectAll.checked = true;
      });
    });

    updateDeleteBtn();
  </script>
</body>
<%- include('../partials/footer') %>
</html>
