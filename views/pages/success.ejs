<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <title>success</title>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460, #2e5e8e);
      background-size: 400% 400%;
      animation: animated-gradient 15s ease infinite;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      color: white;
    }

    @keyframes animated-gradient {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .main-content {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .glass-card {
      width: 100%;
      max-width: 600px;
      padding: 2rem 2.5rem;
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(12px);
      border-radius: 25px;
      border: 1px solid rgba(255, 255, 255, 0.15);
      box-shadow: 0 8px 32px rgba(0, 201, 167, 0.4), 0 0 20px rgba(58, 134, 255, 0.3);
      text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .glass-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 16px 40px rgba(58, 134, 255, 0.5), 0 0 30px rgba(0, 201, 167, 0.4);
    }

    .success {
      font-size: 1.8em;
      color: #7CFC00;
      margin-bottom: 20px;
      font-weight: 600;
    }

    .info {
      margin: 10px 0;
      font-size: 1rem;
    }

    .btn-action {
      display: inline-block;
      margin-top: 25px;
      text-decoration: none;
      color: #fff;
      background: linear-gradient(135deg, #7CFC00 0%, #2e5e8e 100%);
      font-weight: 600;
      border-radius: 8px;
      padding: 0.5rem 1.2rem;
      border: none;
      transition: all 0.3s;
      box-shadow: 0 2px 10px rgba(44,62,80,0.15);
      font-size: 1rem;
    }
    .btn-action:hover {
      background: linear-gradient(135deg, #2e5e8e 0%, #7CFC00 100%);
      color: #222;
    }
  </style>

  <link rel="icon" type="image/png" href="/images/logo_app_big.svg">
</head>
<body>
  <!-- Include headerlogin di dalam body jika perlu -->

  <div class="main-content">
    <div class="glass-card">
      <% if (success) { %>
        <div class="success">✅ <%= message %></div>
        <div class="info">
          <strong>Total komentar diproses:</strong> <%= selectedIds %>
        </div>
        <a href="/dashboard" class="btn-action">⬅️ Kembali ke Dashboard</a>
      <% } else { %>
        <div class="success" style="color:#e94560;">❌ <%= message %></div>
        <div class="info">
          <strong>Total komentar gagal diproses:</strong> <%= selectedIds %>
        </div>
        <a href="/judolremover" class="btn-action" style="margin-right:1rem;">
          <i class="bi bi-arrow-left"></i> Kembali ke JudolRemover
        </a>
        <a href="/dashboard" class="btn-action" style="margin-left:1rem;background:#444;">Kembali ke Dashboard</a>
      <% } %>
    </div>
  </div>

  <!-- Footer tetap di bawah jika perlu -->
</body>
</html>
