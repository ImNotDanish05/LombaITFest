<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <title>Profil Pengguna</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460, #2e5e8e);
      background-size: 400% 400%;
      animation: gradientMove 15s ease infinite;
      display: flex;
      flex-direction: column;
      color: white;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .main-content {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 4rem 2rem 12rem;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(16px);
      border-radius: 25px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 20px 60px rgba(0, 201, 167, 0.4), 0 0 40px rgba(58, 134, 255, 0.3);
      padding: 3rem 4rem;
      max-width: 800px;
      width: 100%;
      display: flex;
      gap: 2.5rem;
      align-items: center;
      position: relative;
      animation: fadeIn 0.6s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .profile-side {
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .profile-img {
      border-radius: 50%;
      border: 4px solid white;
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.4);
      width: 130px;
      height: 130px;
      object-fit: cover;
      flex-shrink: 0;
      background: #fff;
      transition: transform 0.3s ease;
    }

    .profile-img:hover {
      transform: scale(1.05);
    }

    .profile-info {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      color : white;
    }

    .profile-main-row {
      display: flex;
      align-items: center;
      gap: 1.2rem;
      margin-bottom: 1.5rem;
    }

    .profile-username {
      font-size: 1.8rem;
      font-weight: 600;
      color: #7CFC00;
    }

    .profile-email {
      font-size: 1rem;
      color: #ffffff;
      opacity: 0.85;
      margin-top: 0.2rem;
    }

    .info-row {
      margin: 0.5rem 0;
      font-size: 1rem;
      display: flex;
      gap: 0.5rem;
    }

    .info-row .label {
      font-weight: 600;
      color: #ffd369;
    }

    .info-row .value {
      color: #e0e0e0;
    }

    .btn-back {
      margin-top: 2rem;
      background: linear-gradient(135deg, #7CFC00 0%, #2e5e8e 100%);
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 0.3rem 0.6rem; /* dikurangi agar tombol lebih pendek */
      font-size: 0.9rem;
      font-weight: 600;
      cursor: pointer;
      box-shadow: 0 4px 12px rgba(44, 62, 80, 0.25);
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      text-decoration: none;
      min-width: 0;
      width: fit-content; /* memastikan hanya selebar konten */
    }


    .btn-back:hover {
      background: linear-gradient(135deg, #2e5e8e 0%, #7CFC00 100%);
      color: #222;
    }

    @media (max-width: 768px) {
      .glass-card {
        flex-direction: column;
        padding: 2rem 1rem;
        text-align: center;
      }

      .profile-main-row {
        flex-direction: column;
        gap: 0.5rem;
      }

      .btn-back {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/headerlogin', { user }) %>

  <div class="main-content">
    <div class="glass-card">
      <div class="profile-side">
        <img src="<%= user.picture %>" alt="Foto Profil" class="profile-img" />
      </div>
      <div class="profile-info">
        <div class="profile-main-row">
          <!-- <div>
            <div class="profile-username"><%= user.username %></div>
            <div class="profile-email"><%= user.email %></div>
          </div> -->
        </div>
        <h2 style="margin-bottom: 1rem;">Profil Pengguna</h2>
        <div class="info-row">
          <span class="label">Nama:</span>
          <span class="value"><%= user.username %></span>
        </div>
        <div class="info-row">
          <span class="label">Email:</span>
          <span class="value"><%= user.email %></span>
        </div>
        <a href="/dashboard" class="btn-back">
          <i class="bi bi-arrow-left"></i> Kembali
        </a>
      </div>
    </div>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
