<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460, #2e5e8e);
      background-size: 400% 400%;
      animation: gradientFlow 15s ease infinite;
      color: #fff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      text-align: center;
    }

    @keyframes gradientFlow {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .error-box {
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      padding: 3rem 4rem;
      border-radius: 20px;
      max-width: 600px;
      box-shadow: 0 8px 30px rgba(233, 69, 96, 0.4);
      transition: transform 0.3s ease;
    }

    .error-box:hover {
      transform: scale(1.02);
      box-shadow: 0 12px 45px rgba(233, 69, 96, 0.5);
    }

    .error-box h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: #e94560;
    }

    .error-box p {
      font-size: 1.25rem;
      margin-bottom: 2rem;
    }

    .error-box a {
      text-decoration: none;
      color: #7f5af0;
      font-weight: bold;
      font-size: 1.1rem;
      transition: color 0.2s;
    }

    .error-box a:hover {
      color: #a88ffb;
    }

    .error-container {
      position: relative;
      display: inline-block;
    }

    .image-top {
      position: absolute;
      top: -200px;
      left: 50%;
      width: 300px;
      height: auto;
      opacity: 0.9;
      transform: translateX(-50%) translateY(0px);
      transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
      transform-origin: center center;
    }

    .image-top:not(:hover) {
      animation: smoothFloat 6s ease-in-out infinite;
    }

    /* Animasi floating yang smooth dan natural */
    @keyframes smoothFloat {
      0%, 100% {
    transform: translateX(-50%) translateY(0);
    }
      50% {
    transform: translateX(-50%) translateY(-10px);
    }
    }

    /* Hover effect yang smooth */
    .image-top:hover {
        transform: translateX(-50%) translateY(-10px) scale(1.1);
      }

    /* Efek glow subtle saat hover */
    .image-top::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 120%;
      height: 120%;
      background: radial-gradient(circle, rgba(127, 90, 240, 0.15) 0%, rgba(233, 69, 96, 0.1) 50%, transparent 70%);
      border-radius: 50%;
      opacity: 0;
      transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
      pointer-events: none;
      z-index: -1;
      filter: blur(8px);
    }

    .image-top:hover::before {
      opacity: 1;
      transform: translate(-50%, -50%) scale(1.3);
    }

    /* Responsif untuk mobile */
    @media (max-width: 768px) {
      .image-top {
        width: 200px;
        top: -150px;
      }
      
      .error-box {
        padding: 2rem 2.5rem;
      }
      
      .error-box h1 {
        font-size: 2.5rem;
      }
    }

  </style>
</head>
<body>
  <div class="error-container">
    <img src="/images/waguri2.png" class="image-top" alt="Waguri Atas">

    <div class="error-box">
      <h1>Error <%= error.code %></h1>
      <p><%= error.message %></p>
      <a href="<%= user ? '/dashboard' : '/' %>">
        ← Kembali ke <%= user ? 'Dashboard' : 'Beranda' %>
      </a>
    </div>
  </div>
</body>
</html>