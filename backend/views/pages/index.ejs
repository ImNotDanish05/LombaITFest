<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JudolCommentsRemover</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Google Fonts: Inter for a modern look -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

    <!-- Custom Style -->
    <style>
        body {
            font-family: 'Inter', sans-serif; /* Menggunakan font Inter */
            display: flex;
            flex-direction: column;
            min-height: 100vh; /* Memastikan body setidaknya setinggi viewport */
            margin: 0;
            background-color: #f8f9fa; /* Warna latar belakang umum */
        }

        .container {
            flex: 1; /* Memastikan konten mengisi ruang yang tersedia */
        }

        /* Custom Navbar Styling */
        .custom-navbar {
            background: linear-gradient(to right, #0a1a20, #152a33, #1e4150); /* Gradient yang lebih gelap dari footer */
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            padding-top: 1rem;
            padding-bottom: 1rem;
        }

        .custom-navbar .navbar-brand {
            color: #ffffff;
            font-size: 1.8rem;
        }

        .custom-navbar .nav-link {
            color: #c0c0c0; /* Warna teks link default */
            font-weight: 500;
            margin-right: 15px;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        .custom-navbar .nav-link:hover,
        .custom-navbar .nav-link.active {
            color: #ffd369; /* Warna aksen saat hover/aktif */
            transform: translateY(-2px);
        }

        .custom-navbar .navbar-toggler {
            border-color: rgba(255, 255, 255, 0.2);
        }

        .custom-navbar .navbar-toggler-icon {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba%28255, 255, 255, 0.55%29' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
        }
        
        /* Custom Footer Styling */
        .custom-footer {
            background: linear-gradient(to right, #0f2027, #203a43, #2c5364); /* Gradient background yang sudah ada */
            color: #e0e0e0; /* Warna teks yang lebih terang */
            padding: 40px 0; /* Padding lebih banyak */
            box-shadow: 0 -5px 15px rgba(0, 0, 0, 0.3); /* Sedikit bayangan di atas footer */
            border-top-left-radius: 15px; /* Pojok atas kiri bulat */
            border-top-right-radius: 15px; /* Pojok atas kanan bulat */
            position: relative; /* Diperlukan untuk efek pseudo-elemen jika ada */
        }

        .custom-footer h5 {
            color: #ffd369; /* Warna aksen untuk judul */
            font-weight: 700; /* Tebal */
            margin-bottom: 1rem;
        }

        .custom-footer p, .custom-footer small {
            color: #c0c0c0; /* Warna teks lebih lembut */
            font-weight: 300; /* Lebih tipis */
        }

        .custom-footer a {
            color: #ffffff; /* Warna ikon sosial */
            font-size: 1.5rem; /* Ukuran ikon lebih besar */
            transition: color 0.3s ease, transform 0.3s ease; /* Transisi untuk warna dan transform */
            margin: 0 10px; /* Spasi antar ikon */
            display: inline-block; /* Memastikan transform bekerja dengan baik */
        }

        .custom-footer a:hover {
            color: #ffd369; /* Warna aksen saat hover */
            transform: translateY(-3px) scale(1.1); /* Efek sedikit naik dan membesar */
        }

        .social-icons-container {
            display: flex;
            justify-content: center; /* Pusatkan ikon sosial */
            gap: 20px; /* Jarak antar ikon */
            margin-top: 1.5rem;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .custom-footer {
                padding: 30px 0;
            }
            .custom-footer a {
                font-size: 1.3rem;
                margin: 0 8px;
            }
            .custom-navbar .nav-link {
                text-align: center;
                margin-right: 0;
            }
            .custom-navbar .btn {
                width: 80%; /* Tombol di mobile lebih lebar */
                margin: 10px auto;
                display: block;
            }
        }
    </style>
</head>
<body>

    <!-- Header -->
    <%- include('../partials/header') %>

    <!-- Div untuk memberi jarak konten dari fixed-top navbar -->
    <div style="height: 80px;"></div> 

    <div class="container py-5">
        <h1>Selamat datang di Webiste Kami </h1>
        <p>Aplikasi ini akan membawa pengalaman yang luar biasa.</p>
        <div style="height: 700px;"></div> <!-- Placeholder content untuk memastikan footer tetap di bawah -->
    </div>

    <!-- Footer -->
    <%- include('../partials/footer') %>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
